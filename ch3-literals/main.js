let currentTempC = 22;          // rрадусы Цельсия
currentTempC = 22.5;
let targetTempC ;               // эквивалент "let target TempC = undefined ";
let targetTempC , room1 = "conference_room_a", room2 = "lobby";
const ROOM_ТЕМР_С = 21.5, МАХ_ТЕМР_С = 30;

// ! Числа
let count = 10;                 // целочисленный литерал
const bluе = OxOOOOff;          // 16-ти ричный
const umask = 000022;           // 8-миричный
const roomTemp = 21.5;          // десятичное число
const с = З.Оe6;                // экспоненциальное 
const inf = Infinity;
const ninf = - Infinity;
const nan = NaN;                // не число

// ! Строки
let string = 'hello';

// ! Символы
const RED = SymЬol();

// ! null и undefined
const targetTemp = null ;
currentTemp = undefined ;

// ! Объекты
const obj = { } ;

// ! Массивы
const a = [1, 2, 4];

// ! Даты
const now = new Date();

// ! Регулярные выражения
const email = /\b[a-z0-9._-J+@[a-z_-]+(?:\.[а-z]+)+\Ь/;

// ! Отображения и наборы (Map и Set) ???


// ! Преобразование типов данных
const numStr = "33.3" ;
const num = NumЬer (numStr);

const а = parseInt ( "16 volts ", 10);      // преобразование по основанию 10 - 16
const Ь = parseInt ( "За" , 16 ) ;          // преобразование по основанию 16 - 58
const с = parseFloat ( "15.5 kph");         // float всегда по основанию 10 -15.5

const d = new Date();                       // текущая дата
const ts = d.valueOf();                     // числовое значение : миллисекунды с полуночи 1 января 1970 года (ИТС)

const Ь = true ;
const n = Ь ? 1 : О ;

const n = 33.5;                             // 33. 5 - число
const s = n.toString();                     // "33.5" - строка

const arr = [1, true, "hello"];
arr.toString();                             // "1, true, hello"

const n = О;                                // значение "false "
const b1 = !!n ;                            // false
const Ь2 = Boolean(n) ;                     // false

